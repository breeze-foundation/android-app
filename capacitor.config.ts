import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tmac.android',
  appName: 'tmac-android',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
